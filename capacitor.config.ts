import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.vuebase',
  appName: 'vue-base-enterprise',
  webDir: 'dist'
};

export default config;
